for n in range (len(naipe)):
            for v in range(len(valor)):
                self.container.append( Carta[valor[v], naipe[n], cor[n]])

for n in range(len(naipe)):
            for v in range(len(valor)):
                self.container.append( Carta(valor[v],naipe[n],cor[n]))